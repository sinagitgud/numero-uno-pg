[build]
builder = "NIXPACKS"
buildCommand = "cd apps/backend && npx prisma generate && npm run build"

[deploy]
startCommand = "cd apps/backend && npm run db:migrate:prod && node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "numero-uno-backend"
