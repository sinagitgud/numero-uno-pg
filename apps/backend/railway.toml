[build]
builder = "NIXPACKS"
buildCommand = "npm run build --workspace=apps/backend"

[deploy]
startCommand = "npm run db:migrate:prod && node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "numero-uno-backend"
